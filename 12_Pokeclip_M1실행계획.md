# Pokeclip M1 수직 슬라이스 실행 계획

**이 문서의 목적**: 기능명세서 M1(A1·A2(2트랙)·A5 / B1·B2·B3·B6 / D1 / I1 — "OBS에서 쏜 방송이 우리 웹에서 멀티오디오로 재생되고 되감긴다")을 **무엇부터, 어떤 순서로** 실행할지 확정한다. 순서의 논리는 하나 — 계약 → 언블록 스텁 → 최대 리스크 우선 검증 → 병렬 → 가는 관통을 4번 반복. "다 만들고 마지막에 붙이기"의 정반대다.

**같이 볼 문서**: [9_기능명세서](9_Pokeclip_기능명세서.md) 3장(마일스톤) · [11_역할분담](11_Pokeclip_역할분담.md) · [ADR-003](adr/ADR-003_미디어서버_Go.md)

---

## 0단계 — 계약 먼저 (첫 2~3일, 코드보다 먼저)

M1에 필요한 계약 3개만 확정한다. 전부 1번이 초안, 2번이 소비자 관점 리뷰.

| 계약 | 내용 |
|---|---|
| SRT 입력 규약 | `streamid`·`passphrase` **형식(길이·문자셋·발급 규칙)** · 트랙 매핑(**6트랙** — [ADR-017](adr/ADR-017_오디오트랙6트랙.md)) + **저장 위치·조회 경로**: streamid=해시(PG) / passphrase=원문 Secrets Manager, Media가 streamid로 조회·적용 ([ADR-018](adr/ADR-018_스트림키저장분리.md)) |
| B7 세그먼트 계약 | 디렉토리·매니페스트 규격 — 폴백 헤지의 전제 |
| LL-HLS/DVR 재생 규약 | 매니페스트 형태·윈도우 길이·세그먼트 URL·시킹 기준 (역할분담 계약 3번) |

## 1단계 — 언블록 장치 두 개 (첫 주)

| 순서 | 작업 | 담당 | 왜 먼저 |
|---|---|---|---|
| 1-1 | I1 docker-compose 스켈레톤 (PG·Redis·Media 자리) | 1번 (+3번) | 전원의 개발 환경 — 모든 것의 바닥 |
| 1-2 | 정적 세그먼트 스텁 서버 — 미리 만든 CMAF 세그먼트+매니페스트를 계약 규격대로 서빙만 | 1번 (반나절) | 2번이 진짜 Media Origin을 기다리지 않고 플레이어 개발 시작 |

## 2단계 — 3트랙 병렬

**핵심 트릭**: 플러그인 없이 Media Origin부터 검증한다. OBS 내장 커스텀 출력이 SRT를 지원하므로(단일 오디오), 표준 OBS로 쏘면서 B1~B3를 먼저 개발한다 — 최대 리스크(Go 서버)의 판정이 플러그인 완성을 기다릴 이유가 없다.

| 트랙 | 순서 | 담당 |
|---|---|---|
| 미디어 서버 | B1 SRT 수신·디먹스(표준 OBS로 테스트) → B2 CMAF 세그먼트화 → B3 LL-HLS 서빙 → DVR 슬라이딩 윈도우 | 1번 |
| 플레이어 | hls.js LL-HLS 스파이크(스텁 상대로) → DVR 시킹 UI → 멀티오디오 트랙 전환 UI | 2번 |
| 데이터·선행 | compose에 PG·Redis 편입 → 스키마 v0 마이그레이션(broadcasts부터) → B6 이벤트 소비 스텁(수신·로그) → 여유 시 M2 선행(H1 구글 로그인 뼈대) | 3번 |

플러그인은 미디어 서버가 B3까지 선 다음 착수: A1 동시 송출(TS 먹싱) → A2 멀티오디오 2트랙 → A5 자동 재연결.

## 3단계 — 통합 게이트 4개 (순서 고정)

| 게이트 | 검증 서사 | 의미 |
|---|---|---|
| **G1** | 표준 OBS SRT → Media Origin → 웹에서 라이브 재생 (단일 오디오) | 최초 엔드투엔드 관통 |
| **G2** | DVR 1시간 시킹 동작 | ⚠️ **폴백 판정 게이트** (멘토 안건 ③) |
| **G3** | 자체 플러그인 송출 교체 + 2트랙 → 플레이어 트랙 전환 | 멀티오디오 차별점 증명 |
| **G4** | 송출 중 회선 단절 → A5 재연결 복구 + B6 시작/종료 이벤트 발행 확인 | M1 서사 완성 |

## 폴백 규칙 (사전 합의)

G2에 **시간 상한**을 둔다(구체 수치는 멘토와 확정). 상한 초과 또는 DVR 시킹의 구조적 실패 시, B7 세그먼트 계약대로 **Media Origin을 FFmpeg 오케스트레이션으로 교체**하고 M2 이후 일정을 보존한다. 이 전환 결정은 사전 합의 사항이며, 시점 판단은 게이트 결과로만 한다.

## 운영 노트

- M1 기간엔 매일 짧은 싱크 (역할분담 공통 규칙)
- 게이트 통과는 데모 영상 또는 재현 절차로 기록해 심사 증빙으로 재활용한다
